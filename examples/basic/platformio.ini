; End-to-end smoke test: renders the generated icon on the host and checks pixels.
[env:native]
platform = native
; Headers come straight from the library checkout: this keeps sibling
; examples invisible to the Library Dependency Finder.
build_flags =
  -I../../src
  -std=c++11
  -Wall
  -Wextra
